function AddVideoToJson(state) {
  const {list} = state
  return ( 
    <div>
      <h1>Add Video to JSON file</h1>
      {
        console.log(list)
      }
    </div>
   );
}

export default AddVideoToJson;
